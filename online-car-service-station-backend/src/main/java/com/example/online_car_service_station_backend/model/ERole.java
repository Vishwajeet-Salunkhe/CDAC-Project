package com.example.online_car_service_station_backend.model;

// Justification: This enum provides a strongly typed and centralized way to define
// and manage the different user roles (CUSTOMER, ADMIN) in the application.
// Using an enum prevents typos and ensures consistency when assigning and checking roles.
public enum ERole {
    ROLE_CUSTOMER,
    ROLE_ADMIN
}